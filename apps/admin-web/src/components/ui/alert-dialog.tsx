import { AlertDialog as AlertDialogPrimitive } from '@base-ui/react/alert-dialog'
import type React from 'react'
import type { ReactNode } from 'react'
import { Button } from './button'

export function AlertDialog({ trigger, title, description, actionLabel, cancelLabel = '취소', onAction, destructive = false }: { trigger: ReactNode; title: string; description: string; actionLabel: string; cancelLabel?: string; onAction: () => void; destructive?: boolean }) {
  return <AlertDialogPrimitive.Root><AlertDialogPrimitive.Trigger render={trigger as React.ReactElement} /><AlertDialogPrimitive.Portal><AlertDialogPrimitive.Backdrop className="fixed inset-0 z-[70] bg-black/55 transition-opacity data-ending-style:opacity-0 data-starting-style:opacity-0" /><AlertDialogPrimitive.Popup className="fixed left-1/2 top-1/2 z-[71] grid w-[calc(100%-2rem)] max-w-md -translate-x-1/2 -translate-y-1/2 gap-5 rounded-2xl border bg-card p-6 text-card-foreground shadow-2xl transition-[scale,opacity] data-ending-style:scale-95 data-ending-style:opacity-0 data-starting-style:scale-95 data-starting-style:opacity-0"><div className="grid gap-2"><AlertDialogPrimitive.Title className="text-lg font-semibold">{title}</AlertDialogPrimitive.Title><AlertDialogPrimitive.Description className="text-sm leading-6 text-muted-foreground">{description}</AlertDialogPrimitive.Description></div><div className="flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><AlertDialogPrimitive.Close render={<Button variant="outline" />}>{cancelLabel}</AlertDialogPrimitive.Close><AlertDialogPrimitive.Close onClick={onAction} render={<Button variant={destructive ? 'destructive' : 'default'} />}>{actionLabel}</AlertDialogPrimitive.Close></div></AlertDialogPrimitive.Popup></AlertDialogPrimitive.Portal></AlertDialogPrimitive.Root>
}
