name: Dev Infra CI

on:
  push:
    branches: [dev]
    paths:
      - infra/**
  pull_request:
    branches: [dev]
    paths:
      - infra/**

jobs:
  compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker compose -f infra/compose.yaml config
